---
title: "Analysis Reproducibility Investigation"
date: 2026-09-02
date-modified: 2026-09-22
---

**Status:** Evidence-backed investigation. SHIELD calibration is the first
reference workflow for evaluating reproducibility. Exact per-run syphilis-manager
selection is available on `master`; provenance, immutable runtime setup, and a
container canary exist on review branches but have not entered the team's ordinary
calibration path. A bounded container pilot can proceed before deterministic
replay is established. A same-seed replay claim still depends on defining and
verifying the sampler's seed and resume contract.

**Verified against:** `jheem_analyses@c97c8af33447758a1c5c50416812d516092be867`
and its `codex/shield-source-readiness` review branch at
`accd7b64dd0ab1f38f5ccad993a643c3c7544de2`,
`jheem2@90b68ad500c12bdfe8f9dc6616e9a846fb4ae3d1`,
`bayesian.simulations@4e0d13e85857396bb0e6e2ac1d244775b2145f75`,
`distributions@4d71d9644b4439a59210e804520ac8717ae8f079`, and
`locations@5af29ec2a2de3d5bb90a133de1353a53d1067e5f`, plus the SHIELD
container proposal at
`jheem-containers@262c733d5c71ec4aa480dde4131880002105dc31`. The ignored
working cache in one clean `jheem_analyses` checkout was inventoried read-only on
2026-09-02 as a sample of current local state, not as a canonical manifest.
Manager-distribution behavior was additionally reverified at
`jheem_analyses@53f6e5d3073433888e1a63bbb00a8522f4c0f033` and against the
repository's published releases on 2026-09-09. The current calibration, staging,
sampler, and container claims were reverified from source on 2026-09-17.
The SHIELD integration changes below were rechecked against
`jheem_analyses@16eff985372fd65be46871bf164acb6eff14beb3` and
`jheem-containers@262c733d5c71ec4aa480dde4131880002105dc31` on
2026-09-22; the older pinned analysis remains a dated snapshot.

## Assessment

The reproducibility problem is broader than the `cached/` directory. That
directory is where several missing contracts have accumulated.

`jheem_analyses` currently serves at least five roles: application source tree,
environment bootstrap, data-processing workspace, artifact catalog, and runbook.
A run is assembled dynamically from a checkout, a sibling `jheem2` checkout or an
installed package, globally installed support packages, host-dependent paths,
ignored and tracked serialized objects, mutable remote aliases, script constants,
and pre-existing R-session state. Each part is understandable in isolation, but
there is no single record of the resolved combination.

The practical result is that a successful analysis may be repeatable on the
machine where it was run without being reconstructable later or elsewhere. Git
records much of the code, and serialized outputs preserve a substantial model
object, but neither records the complete computation.

The first move should not be a cache-folder reorganization or a wholesale rewrite.
It should be an automatic **run record**: capture what the existing entry point
actually used, report clearly when an identity cannot be established, and make
that record recoverable from every material output. Existing callers should not
need to learn a new API. Resolution into an immutable run lock and clean-workspace
reconstruction can become stricter as the underlying inputs are made identifiable.

## What “reproducible” should mean here

There are several useful levels, and they should not be conflated:

| Level | Question answered | Current state |
|---|---|---|
| Discoverable | What command and inputs are intended? | Partial; knowledge is spread across scripts and a few application-specific notes |
| Repeatable | Can the same operator rerun it in the current workspace? | Often, when the same cache, package library, paths, and session assumptions remain available |
| Reconstructable | Can a clean workspace resolve the same code, data, configuration, and environment? | Not generally |
| Auditable | Can an output state exactly what produced it? | Partial for release-driven delivery workflows; absent for ordinary calibration and simset outputs |
| Scientifically verifiable | Can a rerun be compared under an agreed numerical or statistical tolerance? | Available in a few downstream golden tests and the syphilis manager validation, not end to end |

Bit-for-bit identity across operating systems is not a necessary first standard for
stochastic, compiled numerical work. The useful initial promise is stronger than
“it runs on my machine” and narrower than universal bitwise identity: the same
declared inputs can be reconstructed, randomness is controlled at the supported
boundary, and outputs can be checked against an explicit scientific tolerance.

## The effective runtime contract today

| Contract element | Observed behavior | Consequence |
|---|---|---|
| Entry point | The repository has 15 application directories, 726 R files and 197,347 lines of R. It has no root README; three application trees contain a README/readme, and 22 R scripts parse command-line arguments. | A reader cannot determine the supported command for most applications from the repository root. |
| Source assembly | 366 R files call `source()`. The shared EHE bootstrap loads managers into globals and sources EHE-specific code; application scripts layer more source calls on top. | The loaded program depends on call order, working directory, and existing global state, not only imported interfaces. |
| Engine identity | The committed switch selects source mode. The shared bootstrap sources the sibling `../jheem2`; the SHIELD bootstrap first pulls `jheem_analyses`, then synchronizes the sibling engine to the moving `dev` branch and can hard-reset it before sourcing. Package setup installs GitHub repositories without refs when packages are missing. | The code that executes can change at launch time. A script commit alone does not identify the engine or support-package source. |
| R environment | No repository `DESCRIPTION`, `renv.lock`, or equivalent dependency lock declares the analysis environment. The RStudio project leaves workspace restore, workspace save, and history at their defaults. | Package versions and restored objects can vary between machines and sessions. |
| Paths | The local workspace convention is a `jheem/` directory with sibling repositories under `code/` and scientific storage under `files/`. From the expected `code/jheem_analyses` working directory, the default storage root is `../../files`; on other machines it is inferred as `Q:`, `/Volumes/jheem$`, or `/mnt/jheem_nas_share` from path existence and a short hostname list. Many scripts bypass this root with working-directory-relative or platform-specific paths; 147 R, R Markdown, or Markdown files contain an absolute platform path. | The same source expression can resolve to different inputs or output roots, and changing the launch directory can change the meaning of a relative path. |
| Data managers | Six manager names are cataloged in a tracked serialized metadata file. Five currently fall through to mutable OneDrive links validated by object creation/modified timestamps. Syphilis follows a GitHub `latest` release alias by default, but now also accepts an explicit release tag: the loader resolves the release asset and published SHA-256 digest, stores it separately by version, verifies it before loading, and exposes the resolution on the loaded manager. Separate `data-managers-v*` releases already preserve surveillance and census manager snapshots with published SHA-256 digests, but the runtime loader does not yet route those other managers to the releases. | Syphilis runs can select and identify a known manager without changing the team default. A staged provenance path captures that resolved identity; ordinary runs do not yet persist it in a durable receipt. Other manager inputs still resolve as “current enough.” |
| Smaller derived inputs | Twenty-five serialized objects are tracked under `commoncode/object_for_version_cache/`. Lookup is by model-version label and silently falls back through registered ancestor versions. | Git versions the files, but a run does not state which fallback object it resolved. |
| Local `cached/` | One sampled checkout contained 17 files totaling 483.6 MiB: shared managers, application-specific managers, a mobility extract, merge intermediates, analysis results, and `.bak` copies. The entire directory is ignored. | “Copy the cached folder” transfers an opaque workspace snapshot, not a declared dependency set. |
| Tracked session/artifact state | The repository tracks 58 R serialization files totaling 65.3 MiB, including `.RDataTmp` (67.2 MB compressed; its gzip header reports about 1.46 GB uncompressed) and a saved `.Random.seed` in `first_time_setup/.RData`. | Source history contains runtime/session artifacts whose role and regeneration path are not declared. |
| Run configuration | Calibration stages, locations, iteration counts, thinning, cache frequency, and similar choices are distributed between registries, inline constants, command arguments, and host-derived branches. `bayesian.simulations@4e0d13e` accepts a public cache seed but does not forward it to the internal constructor; the downstream seed parameter is also not used when chunk seeds are generated. | The effective configuration must be reconstructed from code paths. A recorded top-level seed is not yet an implemented replay contract, and the current public cache setup path is internally inconsistent. |
| Outputs | `save.simulation.set()` names an output by model version/sub-version, calibration code, sample count, location, and intervention. The simset contains model and performance metadata, but not repository commits, package versions, data-manager identities, environment identity, or an output digest. A review branch adds readable and JSON sidecars and indexes the artifact digest in a local spool; the serialized simset itself is unchanged. | Ordinary SHIELD runs do not yet produce these receipts. The staged sidecar establishes a candidate contract, but it still needs reconciliation with current `master`, a server trial, and durable archival. Other save paths and legacy outputs remain unidentified. |

These are not independent defects. They are consequences of one missing
abstraction: there is no resolved, immutable description of an analysis run.

### Source references

The repository-wide counts above were taken from tracked and working-tree files at
the pinned `jheem_analyses` commit. The implementation claims are grounded in:

- shared source, manager, and global-state assembly:
  `jheem_analyses:source_code.R:2-90`;
- package installation, manager routing, floating release resolution, and legacy
  timestamp validation: `jheem_analyses:commoncode/cache_manager.R:1-124` and
  `:213-430`;
- host/path inference: `jheem_analyses:commoncode/file_paths.R:3-36`;
- tracked derived-object lookup and ancestor fallback:
  `jheem_analyses:commoncode/cache_object_for_version_functions.R:1-56`;
- first-time package installation from unpinned GitHub repositories:
  `jheem_analyses:first_time_setup/install_packages.R:3-34`;
- workspace restore/save settings: `jheem_analyses:jheem_analyses.Rproj:3-5`;
- SHIELD's Git synchronization and model bootstrap:
  `jheem_analyses:applications/SHIELD/shield_source_code.R:19-183`;
- SHIELD's runtime manager selection, population-year constant, and MSM data
  contract: `jheem_analyses:applications/SHIELD/shield_source_code.R:144-201` and
  `applications/SHIELD/R/shield_specification_helpers.R:88-200`;
- the syphilis source registry and build inputs:
  `jheem_analyses:commoncode/data_manager_sources.json:1-8` and
  `.github/workflows/build-syphilis-manager.yml:5-144,200-274`; the stale optional
  workflow handoff is at `.github/workflows/build-syphilis-sections.yml:110-124`;
- forced structural-baseline updates and unchecked alias promotion:
  `jheem_analyses:.github/workflows/build-syphilis-manager.yml:146-223` and
  `.github/workflows/promote-syphilis-manager.yml:15-65`, plus the attached reports
  on the August 26 and July 27 syphilis-manager releases;
- versioned surveillance/census snapshot publication:
  `jheem_analyses:scripts/upload_manager_deps.R:1-69` and the
  `data-managers-v2026.08.26` release;
- source-mode engine assembly:
  `jheem2:R/tests/source_jheem2_package.R:2-93`;
- simset naming and serialization:
  `jheem2:R/FILE_MANAGER_file_manager.R:64-323`; and
- performance-oriented run metadata: `jheem2:R/JHEEM_run_metadata.R:3-153`.

## SHIELD reference-workflow checkpoint (2026-09-17)

A failed SHIELD stage-3 calibration was initially presented as a shared-storage
interruption because the launcher caught any `run.calibration()` error, replaced
its message with a write-interruption explanation, and retried. Direct checks of
the output roots and lower-level file operations did not reproduce a storage
failure. The same calibration then failed against a local output root, and the
failure was traced to a likelihood term with a zero denominator. This incident
joins three reproducibility questions that were previously being treated
separately: whether the real error remains observable, whether the scientific
inputs and environment can be reconstructed, and whether an interrupted
calibration follows the same stochastic path when resumed.

Current `jheem_analyses` `master` changes the future-growth ratio denominator from
`start` to `start + 1` in
[`55bd55c6`](https://github.com/tfojo1/jheem_analyses/commit/55bd55c6975c09063c8c62828bf52f6a9da31eb3).
That makes the immediate division finite but also defines scientific behavior for
zero and small baselines. Engineering work may reproduce the edge cases, preserve
the original error, and add characterization or non-finite-likelihood checks. A
different formula, pseudocount, exclusion rule, or expected value should not be
selected through an infrastructure change; the intended behavior needs explicit
scientific review before it becomes a regression assertion.

The sampler currently lacks a coherent explicit-seed contract. At
[`bayesian.simulations@4e0d13e`](https://github.com/tfojo1/bayesian.simulations/commit/4e0d13e85857396bb0e6e2ac1d244775b2145f75):

- `create.mcmc.cache(seed = ...)` validates `seed` but does not pass it to
  `do.create.cache()`, whose signature requires it;
- `do.create.chain.cache.controls(..., seed)` accepts the value but generates
  per-chunk seeds with `runif()` from ambient R state rather than from that
  argument; and
- `jheem2::set.up.calibration()` exposes no seed argument, so current application
  setup relies on prior global RNG state.

Per-chunk seed storage was introduced in
[`b170ce7`](https://github.com/tfojo1/bayesian.simulations/commit/b170ce7e5c265fa7540a2847a8bc894fa797dd2c),
after the `34840866` revision currently installed by the container base lock.
Consequently, the existing container canary proves that a partial cache can be
continued to its remaining chunk; it does not prove that an explicit seed controls
the schedule, that interrupted and uninterrupted traces are equal, or that the
same request is reproducible across the team's servers.

Two review branches establish useful but incomplete evidence:

- [`jheem_analyses@accd7b64`](https://github.com/tfojo1/jheem_analyses/commit/accd7b64dd0ab1f38f5ccad993a643c3c7544de2)
  stages automatic provenance, immutable source declarations, read-only manager
  caches, truthful failure/resume behavior, and a bounded SHIELD canary. It split
  from `master` at `ea111ea3` and must be reconciled with later application work,
  including the likelihood change, before it can be proposed for ordinary use.
- [`jheem-containers@262c733`](https://github.com/ncsizemore/jheem-containers/commit/262c733d5c71ec4aa480dde4131880002105dc31)
  builds a pinned SHIELD image and has completed a real-engine preflight plus a
  forced-interruption/resume canary in CI. The image still pins the older sampler,
  the canary is a tiny stage-0 path rather than the failing stage-3 path, and no
  uninterrupted-versus-resumed or cross-server equality comparison is made.

These are validation assets, not yet evidence that SHIELD calibration is
reproducible. The 2026-09-17 sequence prioritized a small, executable sampler
seed demonstration and focused correction before SHIELD branch reconciliation.
The narrower container-pilot sequence is updated below; it does not remove the
sampler prerequisite for a deterministic-replay claim.

## SHIELD container-pilot update (2026-09-22)

The team is evaluating containerization of SHIELD as an operational deliverable.
That question is narrower than whether an earlier calibration can be reproduced
exactly. The [existing container spike](https://github.com/ncsizemore/jheem-containers/blob/262c733d5c71ec4aa480dde4131880002105dc31/workloads/shield/README.md)
builds a pinned image and exercises a small real-engine calibration through a
forced interruption and checkpoint continuation in hosted CI. It has not yet
run on a team server, completed production-sized simset assembly there, or
compared uninterrupted and resumed scientific traces.

The spike pins `jheem_analyses@accd7b64`, while the verified active branch is
`master@16eff985`, 71 commits ahead of the common base with the staged
`codex/shield-source-readiness` branch. The active SHIELD tree now has a
[three-tier model-test workflow](https://github.com/tfojo1/jheem_analyses/blob/16eff985372fd65be46871bf164acb6eff14beb3/.github/workflows/test-shield.yml)
and a [calibration launcher](https://github.com/tfojo1/jheem_analyses/blob/16eff985372fd65be46871bf164acb6eff14beb3/applications/SHIELD/shield_calib_setup_and_run.R)
that prints the original error and stops on many non-storage failures. Those
changes supersede parts of the staged branch, but ordinary setup still defaults
to clearing state and uses a fixed `set.seed(0)` before calibration. The
[source bootstrap](https://github.com/tfojo1/jheem_analyses/blob/16eff985372fd65be46871bf164acb6eff14beb3/applications/SHIELD/shield_source_code.R)
still requires a sibling `jheem2` checkout on `dev` in source mode, and can pull
when interactive. A recorded container run needs an explicit, non-mutating
source and run-mode contract; the older branch supplies candidate pieces, not a
merge-ready replacement for the current application.

The new test workflow is useful characterization, not yet a dependable green
gate: the [2026-09-22 run](https://github.com/tfojo1/jheem_analyses/actions/runs/35762822719)
failed in the static tier and skipped the later tiers. Its
[README](https://github.com/tfojo1/jheem_analyses/blob/16eff985372fd65be46871bf164acb6eff14beb3/applications/SHIELD/tests/README.md)
says some tests intentionally fail to preserve open code-review findings. At
least one static test asserts that the `+1` growth-penalty denominator should be
removed and suggests alternatives; because that is a scientific formula choice,
it should be resolved by the scientific owners, not silently changed as
container-readiness work.

A clean local checkout of the same pinned analyses commit reproduced 11 static
assertion failures and 3 static skips. Running the unit-file subset independently
returned 6 assertion failures, 4 errors, and 4 skips; missing test-bootstrap
inputs account for some of those errors and skips, so these counts are not a
model-defect count. The workflow chains static → unit → integration with `needs`,
which means a persistently red static tier prevents the model tier from running.
Conversely, the
[test runner](https://github.com/tfojo1/jheem_analyses/blob/16eff985372fd65be46871bf164acb6eff14beb3/applications/SHIELD/tests/run_tests.R#L94-L114)
exits nonzero only for failed assertions or errors: if required bootstrap stages
are unavailable and tests only skip, a tier can appear green without exercising
its intended model path. Before using this workflow as a container acceptance
gate, separate open diagnostic findings from must-pass checks and make missing
required integration prerequisites fail explicitly. Text matching for a local
`set.seed()` call and the growth-penalty formula test are examples that need a
contract-level or domain-reviewed assertion rather than an automatic code edit.

A bounded pilot should therefore establish, in order: a pinned image built
against current SHIELD source; identified manager assets and source revisions;
non-destructive fresh/resume behavior; successful real-engine preflight and a
small checkpoint continuation; then a retained run on a team server that checks
mounts, file ownership, error logs, runtime, and output inspection. Full assembly
and repeated-run comparisons are separate acceptance steps. The sampler seed
finding remains recorded and should be tested before claiming deterministic
replay or equivalent results across servers.

## The current filesystem contract

The common local checkout is organized as:

```text
jheem/
|-- code/
|   |-- jheem2/
|   |-- jheem_analyses/
|   |-- bayesian.simulations/
|   `-- ...
`-- files/
    |-- mcmc_runs/
    |-- mcmc_summaries/
    `-- simulations/
```

This is partly a convention and partly an implemented API. `source_code.R` assumes
that `jheem2` and `jheem_analyses` are sibling repositories, sources
`commoncode/file_paths.R`, and places the selected storage root into jheem2's
process-global root-directory setting. On a local checkout that root is
`../../files`, relative to the expected `code/jheem_analyses` working directory.
On a shared desktop, macOS NAS mount, or modeling server, the equivalent storage
root is instead `Q:`, `/Volumes/jheem$`, or `/mnt/jheem_nas_share`; these roots
contain the scientific subdirectories directly rather than a second `files/`
level.

jheem2 defines three standard locations beneath that root:

- `mcmc_runs/<version>/<location>/<calibration-code>/` for calibration state;
- `mcmc_summaries/<version>/<calibration-code>/` for assembled summaries; and
- `simulations/<version[-subversion]>/<calibration-code>-<n>/<location>/` for
  simsets.

The convention is not comprehensive. The analysis repository's ignored
`cached/` directory is inside the code checkout, not the scientific storage root,
and application scripts also write to paths such as `results/`, `run_results/`,
and application-specific directories. Consequently, “JHEEM root” currently means
a jheem2 scientific-output root, not an unambiguous workspace, source, input, or
archive root.

Provenance should preserve a logical artifact role and stable identity separately
from its physical location. The existing directory tree can remain a compatibility
adapter, but a relative path or NAS mount point is not an artifact identity. A run
record should capture both the logical reference and the resolved location used
for that attempt.

## The cache is three different things

The name `cached/` currently hides distinct lifecycles:

1. **Published inputs** — data managers that analyses depend on and that should
   have stable versions, checksums, lineage, and compatibility metadata.
2. **Local materializations** — downloaded copies that should be disposable and
   safely reconstructable from a registry or manifest.
3. **Working products** — merge intermediates, exploratory results, backups, and
   other outputs whose retention policy belongs to the producing workflow.

There is also a fourth cache outside that directory: small, tracked
model-version-keyed serialized objects in `commoncode/object_for_version_cache/`.
Those behave more like versioned application inputs than disposable caches.

Renaming these directories would improve legibility but would not make a run
reproducible. The same ambiguity would persist under better names until acquisition,
identity, ownership, and output rules are explicit.

## Existing foundations

The ecosystem already contains most of the patterns needed:

- Git can identify application and package source exactly.
- Versioned `data-managers-v*` releases already provide rollback material for
  surveillance and census managers. Nine dated snapshots were published between
  2026-03-10 and 2026-08-26; the
  [`data-managers-v2026.08.26`](https://github.com/tfojo1/jheem_analyses/releases/tag/data-managers-v2026.08.26)
  release contains a 44.4 MB surveillance manager and 108 MB census manager with
  published SHA-256 digests. These are currently uploaded snapshots used as build
  dependencies, not manager builds protected by active-model compatibility
  checks, and the ordinary runtime loader does not expose them as selectable
  versions.
- The syphilis-manager workflow declares major inputs, builds in CI, performs
  structural validation, and publishes a versioned release. It is the closest
  upstream exemplar, but its approval boundary is incomplete. A manual override
  can convert structural regressions into a new baseline, and the separate
  promotion workflow verifies only that the requested release exists. The current
  approved alias consequently points to a build whose attached report says
  `FAIL (17 failures, 10 warnings)`. Its defaults are also floating aliases
  (`*-latest`, `jheem2_ref=dev`), and its release notes record the requested
  `jheem2` ref rather than the resolved engine commit. The optional handoff from
  the section-build workflow still dispatches the filename of a workflow now
  stored under `.github/workflows-archived/`, so that path also needs repair and
  an end-to-end verification before it is treated as automatic.
- The delivery platform pins package refs and promotes tested container digests.
- The calibration cache already has an identity and checkpoint structure, and the
  proposed observability sidecar can share a run identity rather than invent a
  second identity system.
- Simset filenames already capture a useful subset of the scientific
  configuration.

This is therefore a consolidation problem, not a greenfield platform project.

## Manager compatibility and rollback

A manager can be internally consistent while being incompatible with a model that
consumes it. Changing an outcome's dimensions, source labels, ontology, location
coverage, or available years can leave the manager structurally valid but break a
specification, likelihood, or functional form. Manager validation and
model-manager compatibility are therefore separate gates.

### Reproduced SHIELD incompatibility (2026-09-09)

SHIELD names its runtime syphilis artifact `SURVEILLANCE.MANAGER`. The bootstrap
loads `syphilis.manager.rdata`, not `surveillance.manager.rdata`, into that global.
The model's MSM initialization then makes two `proportion.msm` requests that
include `sex = "male"`: an Emory county request and a BRFSS state/race request.

At the time the failure was reported, `syphilis-manager-latest` resolved to
[`syphilis-manager-v2026.08.26`](https://github.com/tfojo1/jheem_analyses/releases/tag/syphilis-manager-v2026.08.26),
SHA-256
`45446020e779f67ad58a0238ba8c505973dd9c8258d25caf84dd863033e2ace9`.
Its attached structural report already identifies the relevant regression:
Emory `proportion.msm` lost `year__location__sex`, while totals-level
`year__location` was added. Three sex-stratified BRFSS shapes also lost most state
locations. The build used the explicit force-update path and was subsequently
promoted to the approved alias.

A read-only probe against the exact release artifact reproduced the application
failure. For Baltimore, the current SHIELD Emory request returns `NULL`, which
triggers `Cannot get best-guess msm proportions`. The same request against
[`syphilis-manager-v2026.07.27`](https://github.com/tfojo1/jheem_analyses/releases/tag/syphilis-manager-v2026.07.27),
SHA-256
`0d9bf7e02d58554a52844bdce85e0506c99aec27ac578d052f6b4d2eb89339eb`,
returns the expected seven-county array. That earlier artifact's structural
report records 687 checks passed and no failures.

Later on September 9,
[`jheem_analyses@cdd7ed7`](https://github.com/tfojo1/jheem_analyses/commit/cdd7ed7c84c045bba7ce9ffe9c5152e0debef8d9)
deliberately removed `sex` from the remaining national BRFSS
`proportion.msm` records and manager writes. The construction still calculates
MSM numerators over male-only denominators; totals-level describes the stored
dimensions, not an all-sex denominator. The resulting
[`syphilis-manager-v2026.09.09`](https://github.com/tfojo1/jheem_analyses/releases/tag/syphilis-manager-v2026.09.09),
SHA-256
`c3e3c983d6b4e9c961f735f9c59d45483bd63cfa715cf75c1fae874da2d129e6`,
was promoted to `syphilis-manager-latest`. Its attached report records 698
structural checks passed, 3 failed, and 3 warnings; the failures are the three
removed BRFSS sex-stratified shapes. The structural baseline was force-updated
after the report was generated.

The September artifact returns `NULL` for both existing SHIELD MSM requests and
returns the expected Emory and BRFSS arrays when `sex` is omitted. A code-only
adjustment that removes `sex` completed `get.best.guess.msm.proportions()` for all
ten routine SHIELD MSAs. A second prototype prefers that totals-level query but
falls back to the earlier `sex = "male"` shape; it completed the same calculation
against the July, August, and September artifacts. The census population request
for 2010 also succeeded against the published August 26 census manager.

The reported 2010 issue is therefore not an independently reproduced missing-data
failure in this code path: `DEFAULT.POPULATION.YEARS` selects the census year,
while the two `proportion.msm` requests average their available years. This probe
establishes the totals-level shape as the intended current manager contract. It
does not establish numerical scientific equivalence: the derived SHIELD MSM
proportions are identical between the August and September artifacts for all ten
MSAs but differ materially from July. For Baltimore, the three race-level values
change from approximately `0.0493`, `0.0431`, and `0.0518` in July to `0.0331`,
`0.0316`, and `0.0366` in August and September. Those value changes remain a
domain-review question rather than an automatic consequence of accepting the new
storage dimensions.

The full compatibility path was then exercised locally with `jheem2@90b68ad`
installed as package version `1.12.3.9000`. For each manager version, a clean
runtime initializer loaded explicit manager artifacts into a temporary output
root; a Baltimore engine compiled, one deterministic median-parameter simulation
completed, and the stage-0 and stage-1 likelihood sets instantiated and returned
6 and 12 finite values respectively. This establishes executable compatibility
of the July, August, and September releases with the prototype adapter. The exact
September artifact also passed the ten-MSA contract and full Baltimore smoke. This
does not select backward compatibility policy, certify changed estimates, or
replace calibration-scale validation.

The immediate rollback contract should allow an operator or run configuration to
select a dated manager release and asset. A convenient alias may identify the
currently approved manager, but it must resolve to a dated tag and digest before
execution and that resolved identity must enter the run record. Failure of a new
candidate should not silently substitute an older manager: either the approved
alias remains on the prior version or the operator explicitly selects a known
version.

The corresponding promotion path is:

1. publish an immutable candidate manager with its digest;
2. run manager-internal structural validation;
3. run compatibility checks for the supported active models against pinned code
   revisions;
4. publish a readable compatibility result; and
5. move the approved alias only when the required checks pass.

The first model check can be deliberately narrow: resolve the candidate syphilis
manager and its surveillance/census dependencies by immutable tag and digest,
build the SHIELD specification and likelihoods, and evaluate a small calibration
path for a representative location in a temporary output root. Fast assertions
should first exercise the two MSM pulls, their ontology mappings, 2010 census
coverage, and all supported SHIELD locations. The end-to-end Baltimore smoke then
catches assumptions not visible in the schema contract. This gates promotion of
an approved manager alias, not ordinary source commits or the creation of
candidates. A forced structural-baseline update may still publish a review
candidate, but it must not by itself make that candidate eligible for promotion.

### Implementation status (2026-09-09)

Exact per-run syphilis-manager selection is implemented on `jheem_analyses`
`master` in
[`87fdd1d9`](https://github.com/tfojo1/jheem_analyses/commit/87fdd1d9604915536f399b3152464fafb3a62732).
Omitting the new final `release.tag` argument retains the existing promoted-manager
path. Supplying an immutable tag resolves that exact GitHub Release asset and its
published SHA-256 digest, materializes it under a version-specific cache path,
verifies the digest before loading, and attaches the resolved identity to the
loaded manager. Offline loading requires an already cached artifact whose metadata
and digest both validate; it does not contact GitHub or substitute another version.
The focused regression script covers legacy positional dispatch, exact and alias
resolution, concurrent cache access, corruption detection and repair, and digest
mismatch rejection. An end-to-end check also loaded the July 27 historical manager
through the public entry point and reloaded it offline.

SHIELD's bootstrap gained an opt-in selector in
[`11b70172`](https://github.com/tfojo1/jheem_analyses/commit/11b701726d74608e3c5fc2c20c3c9cb6d1bc6ac1).
A run can set `SYPHILIS.MANAGER.RELEASE.TAG` before sourcing the bootstrap or use
`JHEEM_SYPHILIS_MANAGER_TAG` in a batch environment. With neither set, SHIELD
continues to load the promoted manager. If a manager is already present, the
bootstrap preserves it and warns rather than pretending the requested selector
took effect. Repository-local usage documentation lives beside the loader in
`commoncode/data-manager-releases.md`.

An unmerged `jheem_analyses` proposal, now based four upstream commits behind
`master`, separates the work into two commits.
Commit
[`2bac62bc`](https://github.com/tfojo1/jheem_analyses/commit/2bac62bcfcdeda516c92a45f1bf249c6a75d5505),
published on the `codex/shield-manager-compatibility-gates` review branch,
implements the non-mutating runtime initializer, ten-MSA
contract, Baltimore smoke, and build/promotion gates described above without
changing the two SHIELD manager queries. The tests use the installed package
path; CI installs the exact requested `jheem2` revision. The promotion proposal
also requires explicit immutable census-manager and `jheem2` inputs and refuses
to move the approved alias unless the candidate's structural report and both
SHIELD checks pass. With unchanged application behavior, that commit rejects the
August 26 artifact in all ten MSAs and accepts July 27; July also passes the full
Baltimore smoke.

The review branch has not been merged or exercised in GitHub Actions. A second,
local-only commit (`42d5f1f5`) layers the dual-shape SHIELD adapter described
above on top. It was deliberately not published while the data and model changes
were being coordinated. Upstream now establishes the totals-level current
contract, but whether current SHIELD should also load July-era managers remains a
separate compatibility-policy choice. The shipped release selector supplies the
rollback mechanism without making that choice automatically or changing the
current MSM queries.

### Current SHIELD adapter (2026-09-22)

The preceding paragraphs describe the September 9 review state, not the current
application. In
[`8c3021ce`](https://github.com/tfojo1/jheem_analyses/commit/8c3021ce5e65dd3d6bcc8a8fbee44524f6da78f2),
SHIELD's `get.best.guess.msm.proportions()` gained both query shapes: it omits
`sex` for managers whose `last.modified.date` is on or after August 1, 2026,
and keeps `sex = "male"` for earlier managers. The current
[source bootstrap](https://github.com/tfojo1/jheem_analyses/blob/16eff985372fd65be46871bf164acb6eff14beb3/applications/SHIELD/shield_source_code.R#L14-L20)
explicitly selects `syphilis-manager-v2026.07.27` in source. The selector still
provides the technical rollback mechanism, but SHIELD's active default is not
the promoted alias at this revision.

The date threshold is an implementation proxy for schema, not an explicit
schema/version contract. This pass verified the code path, not the behavior of
that current adapter against all three published manager artifacts or the
ten-MSA/Baltimore probes. Those checks should be rerun before making the
unmerged promotion gate authoritative. The earlier value differences remain a
separate scientific interpretation question.

## Proposed target contract

Every supported run should begin from a small declarative request and produce a
resolved lock plus a receipt. Existing public entry points such as
`set.up.calibration()`, `run.calibration()`, intervention runners, and
`save.simulation.set()` should call the provenance layer internally. The exact
syntax and package ownership are implementation choices; automatic capture with
no new caller ceremony is part of the contract.

### Before execution: resolve and preflight

A resolver should turn a request such as an application, operation, location, and
calibration stage into immutable identities:

- `jheem_analyses`, `jheem2`, `bayesian.simulations`, `distributions`, and
  `locations` commits or release versions;
- R version plus an environment lock or container digest;
- every data-manager and auxiliary-input release, asset name, and SHA-256 digest;
- the complete run configuration, including calibration settings and the seed
  contract supported by that software version;
- the output root and a unique run ID.

Preflight should report missing or incompatible inputs without installing packages,
pulling branches, resetting checkouts, or starting an expensive model run. A
separate, explicit materialization command may download verified assets into a
local cache.

### During and after execution: run history and receipt

The resolved lock should be written when the run is created and remain immutable.
Each attempt or chain should write append-only status events that refer to its run
ID. Completion should add an output receipt with artifact paths, sizes and
digests, terminal status, timestamps, and the verification performed. This gives
an operator a readable answer to “what ran, where did it finish, and can its
inputs be identified?”; JSON is the machine-readable implementation, not the
user-facing milestone.

The provenance record should be a small sidecar, not another field embedded only
inside an R6 serialization. It must remain readable without loading the exact
package version that created the science object.

### Artifact-led discovery

The forensic starting point will often be an output rather than a run directory.
Given a new simset or other promoted artifact, it should be possible to recover
the run record without knowing which server or directory produced it. Three links
serve different failure modes:

1. the science object carries a minimal run ID, schema version, and immutable
   pre-run lock hash;
2. a readable provenance sidecar is written adjacent to the artifact; and
3. the durable archive maps the artifact's SHA-256 digest back to its run ID.

The full receipt should not exist only inside a version-sensitive R serialization.
Conversely, embedding the entire environment record in every large artifact would
create unnecessary duplication and make later schema evolution harder. Legacy
artifacts should be handled by a forensic inspector that reports recoverable facts
and marks the rest as unknown rather than manufacturing certainty.

The staged SHIELD slice implements links 2 and 3 without changing simset
serialization. Its adjacent sidecar contains the context and completed receipt;
the local spool indexes one or more originating run records under the artifact
digest. Link 1 remains open. `load.simulation.set()` reconstructs a copy of the
serialized R6 object, so an arbitrary object attribute would not by itself be a
reliable embedded reference; any in-object run ID needs an explicit, supported
field and compatibility policy in `jheem2`.

### Capture and archive

Capture must not depend on a mounted NAS or a live network connection. Each
process should first write atomically to a machine-local spool and, when an
artifact is produced, write its adjacent receipt. An asynchronous archiver then
copies immutable records to a configured durable sink. The record's archive state
should be explicit (`pending`, `archived`, or `failed`), with idempotent retry on a
later run or by a lightweight background task.

The storage backend remains an open implementation choice:

| Candidate | Appropriate role | Constraint |
|---|---|---|
| Backed-up NAS directory | Initial shared archive when mounted | A mount is not guaranteed, and NAS placement is not durability until backup, snapshot, retention, and restore behavior are verified |
| Dedicated Git repository | Potentially near-zero incremental-cost archive and human-readable history for small finalized receipts | Concurrent run processes should not push directly. A single archiver or bot should batch immutable per-run files; binaries and frequently changing status do not belong in the repository |
| Versioned object storage | Natural long-term store for immutable records and digest-keyed lookup | Requires an approved account, credentials, retention configuration, and an acceptable ongoing cost |

Git is therefore a credible first archive backend at the expected metadata scale,
but it should implement a generic archive interface rather than define the run
contract. The schema and capture code must work unchanged if the archive later
moves to object storage. Per-run records should use distinct paths; shared indexes
should be generated by one writer rather than updated by every worker.

The science process should not fail merely because archival is temporarily
unavailable. Enforcement can tighten by lifecycle: exploratory runs may complete
with a visible pending archive; expensive routine runs should emit a prominent
warning; an artifact must have a complete, archived receipt before it is promoted
or published.

### Storage roles

The resulting layout can distinguish four roles without requiring an immediate
physical migration:

| Role | Contract |
|---|---|
| Source data | Immutable release or controlled snapshot with identity, checksum, and access rules |
| Built scientific input | Versioned data-manager or auxiliary artifact with lineage and compatibility metadata |
| Local cache | Disposable materialization keyed by artifact identity and verified on use |
| Run workspace/output | Unique run directory containing resolved lock, status/logs, checkpoints, outputs, and receipt |

An adapter can initially map the existing `cached/<friendly-name>` files into
these roles. That makes current work visible without pretending that every legacy
file already has provenance.

## Recommended sequence

### Current SHIELD reproducibility slice

Work should proceed in dependency order and produce a small, reviewable artifact
at each boundary:

1. **Make failures truthful without selecting science.** Preserve the original
   calibration condition and add a diagnostic probe covering ordinary, zero, and
   near-zero likelihood inputs. Do not alter likelihood formulas or encode the
   `start + 1` policy as expected behavior until the scientific owners confirm it.
2. **Establish the sampler seed contract.** Publish a short explanation and an
   executable reproducer showing whether the same explicit setup seed yields the
   same starting state and chunk-seed schedule. Correct the forwarding and local
   RNG behavior in `bayesian.simulations`, with focused same-seed, different-seed,
   uninterrupted/resumed, and changed-process-RNG tests.
3. **Reconcile the SHIELD readiness branch.** Rebase or reconstruct its bounded
   runtime, provenance, and canary commits on current `jheem_analyses` `master`.
   Likelihood behavior changes remain outside this infrastructure slice.
4. **Run the deterministic comparison.** Under one recorded Linux image digest,
   compare a fresh uninterrupted run with an interrupted/resumed run, then repeat
   one seed on each server. Compare the stored seed schedule, starting parameters,
   initial likelihood, sample and likelihood traces, accept/reject decisions, and
   final chain state; exclude timestamps and runtime measurements.
5. **Pilot the supported path.** Run a bounded production-representative SHIELD
   calibration against explicit managers and roots, inspect the human-readable
   run summary, and verify that the artifact can lead back to its receipt.
6. **Add durable archival.** Retain local-first capture and add one serialized,
   retryable writer to the selected near-zero-cost shared sink. Promotion or
   publication can eventually require an archived receipt; exploratory science
   should not fail because the archive is temporarily unavailable.

Calibration monitoring follows this slice. It uses the same checkpoint and seed
boundary, so implementing stop decisions first would make both systems harder to
reason about.

### Immediate model-manager compatibility slice

Before building the general provenance layer, use the current compatibility need
to establish its first real input contract:

1. reproduce the active-model failure against the candidate manager and verify a
   dated prior release as a rollback candidate;
2. extend the loader to select a dated release and verify its asset digest without
   requiring manual file replacement;
3. add the narrow SHIELD compatibility smoke in a temporary root; and
4. require that smoke before moving the approved manager alias.

This slice provides an immediate recovery path and produces the first concrete
manager identity and compatibility result for the run record. Full build
automation for surveillance and census managers can follow; it is not a
prerequisite for making the existing snapshots selectable and testable.

### 1. Define the minimal record and build a read-only collector

For one active path, report the current analyses and sibling-package commits,
dirty state, R and package versions, resolved roots, required manager names, local
versions or timestamps, and whether each identity is exact, floating, or unknown.
It should make no network, Git, package-library, or cache changes.

The SHIELD calibration path is a strong first candidate because it exercises the
current shared-server workflow, syphilis's release path, legacy census-manager
resolution, source-mode loading, and the calibration work already under
investigation. The doctor can first observe the current launcher without changing
its scientific behavior. This bounded choice would not select SHIELD as the
ecosystem's eventual reference application.

### 2. Add an automatic run receipt to a shadow calibration

Have calibration setup create a format-versioned run ID and initial record;
`run.calibration()` should record attempts and terminal state; and assembly or
`save.simulation.set()` should write the artifact receipt. Preserve the existing
caller API. Initially, unresolved inputs should be recorded as `unknown` with a
reason; that is more honest and more useful than blocking all legacy work. New
runs should progressively require exact identities.

The first visible deliverable should be a concise run summary, not a raw manifest:
what was requested, which location and calibration ran, when and where it ran,
whether it completed, which artifact it produced, and which dependencies are
exact, floating, or unknown. Focused regression checks should prove that capture
does not consume randomness or change calibration results, cache resumption, or
simset serialization.

**Staged baseline (2026-09-17).**
[`jheem_analyses@da62c1ca`](https://github.com/tfojo1/jheem_analyses/commit/da62c1cab338fc63534641bd4831d04485ba4a5a)
adds a read-only context collector, calibration run IDs, immutable setup records,
per-process append-only attempt events, completed artifact receipts, readable
summaries, adjacent simset sidecars, and digest-led lookup from a machine-local
spool. It records the actual calibration cache configuration, per-chain starting
parameters, and any chunk seeds exposed by the installed `bayesian.simulations`
version. Identity is classified as exact, modified, version-only, floating, or
unknown; repository URL userinfo is removed before persistence.

[`jheem_analyses@acc8e416`](https://github.com/tfojo1/jheem_analyses/commit/acc8e416485fbbfaf5313e6d634ecbbb3d67eb67)
wires that layer into both supported SHIELD calibration launchers without new
arguments. Setup, chain execution, and assembly may occur in separate processes;
they coordinate through the existing calibration directory, and parallel chains
write separate event files. Provenance failures warn rather than terminating the
scientific run, and capture can be disabled explicitly for troubleshooting. These
commits are published on the `codex/shield-source-readiness` review branch, not
ordinary `master`.

The focused regression script verifies unchanged R random-number state, cache
configuration and seed capture, split-process/concurrent event writes, adjacent
receipts, digest-led recovery including multiple origins for byte-identical
artifacts, no-op opt-out, and survival of the primary record when the local spool
is unavailable. It also confirms that the artifact bytes are not modified. This
is not yet a production observation. Before a server trial, the branch must be
reconciled with current `master` and the sampler seed contract must be made
testable; otherwise the receipt would faithfully record an environment whose
stochastic replay semantics remain undefined. The local spool is an outbox; no
automatic shared archiver exists yet.

### 3. Separate resolution from mutation

Move `git pull`, branch switching/resetting, package installation, and artifact
download out of model sourcing. Bootstrap should validate a prepared workspace;
an explicit setup command should make changes. This makes “load the model” a
deterministic operation and prevents long runs from silently selecting newer code.

### 4. Make the environment reconstructable

Lock the environment used by the selected path. An `renv` lock may be appropriate
for interactive and server R workflows; an image digest may be better for batch
execution. The choice should follow a small feasibility test because source-mode
C++ compilation makes the compiler and system libraries part of the environment.
A repo-wide lock should not be imposed until it is clear that all 15 applications
can share one dependency set.

### 5. Graduate managers into an artifact registry

Extend the syphilis release pattern to record resolved source commits, immutable
input tags, checksums, and compatibility. Then graduate census and surveillance
managers. A friendly `latest` alias can remain for convenience, but the resolver
must translate it to an immutable release and digest before a run starts.

### 6. Attach receipts to material outputs

Teach the supported save/publish path to write a provenance sidecar and validate
that it agrees with the resolved lock. Embed only the stable run reference and
pre-run lock hash in the science object, then record and index the completed
output's digest in the receipt and archive. This ordering avoids a circular hash
dependency between an artifact and the receipt that identifies it. Begin with
calibration/simset output for the reference path, then carry the same run identity
through extraction and release.

### 7. Expand by demonstrated workflows

Add one supported command and one reconstruction check at a time. Each migrated
application supplies evidence about which pieces are genuinely common. Only then
should shared scaffolding replace copied scripts broadly.

Before broad rollout, exercise the failure modes directly: run while the NAS and
network are unavailable and archive later; interrupt a chain between checkpoints;
copy a simset without its sidecar and recover the record by embedded run ID or
artifact digest; compare two runs that differ in one declared input; and inspect a
receipt on a machine without JHEEM loaded.

## What not to do first

- Do not make a cleaner `cached/` tree the deliverable. It would rearrange unknowns.
- Do not require every historical artifact to acquire retroactive provenance.
  Mark legacy identity as unknown and establish the rule for new artifacts.
- Do not begin with a universal launcher for all 15 applications. Establish the
  contract on one real workflow first.
- Do not equate an `renv.lock` with end-to-end reproducibility. It does not identify
  data, source-mode sibling checkouts, run configuration, randomness, or outputs.
- Do not make ordinary model sourcing update Git or install software.
- Do not have every chain commit and push operational state to a shared Git
  branch. Local capture and archival are separate responsibilities, and a Git
  backend needs a serialized writer.

## Open questions

- Which bounded SHIELD run is representative enough for the first same-image,
  cross-server comparison without incurring a full calibration cost?
- After exact equality is tested within one Linux image and architecture, which
  scientific outputs and tolerances define equivalence across materially different
  numerical platforms?
- What scientific behavior is intended for the future-growth penalty when its
  baseline is zero or near zero?
- Can all required scientific inputs be stored as release assets, or do some need
  an access-controlled artifact registry with the same identity/checksum contract?
- Which package and system dependencies differ across active applications enough
  to require per-application environments?
- Which current outputs are durable scientific artifacts, and which are disposable
  intermediates or personal exploratory products?
- What durable sink can every modeling server reach, at acceptable cost, and what
  service identity should archive on behalf of runs? If the first sink is Git,
  where will the single writer run and how will pending local spools be discovered?
- Is the NAS independently backed up and routinely restorable, and what retention
  applies to calibration state, simsets, and provenance records?
- Which entry point should retry pending archives on machines without an always-on
  archiver?
- Should the tracked `.RDataTmp`, `.RData`, and saved setup workspace be removed
  after their contents and any historical value are reviewed?

## Durable home and exit condition

This page remains the cross-repository investigation record while the contract is
evaluated. If adopted, the collector, archive adapter, supported commands,
application requirements, and environment setup belong in `jheem_analyses` or a
small owning package; package-specific compatibility metadata belongs with the
owning packages; and artifact receipts travel with their releases and outputs.
The wiki retains the cross-repository contract and decision rationale.

The investigation graduates when one production-representative path can be
prepared from declared inputs in a clean workspace, emits a provenance receipt,
and is documented in its owning repositories. It should be retired if the team
selects a different reproducibility boundary.
